(function () {
    'use strict';

    angular
        .module('pcoApp')
        .controller('ToolbarController', [
        	function()
            {
                this.msg = 'Toolbar mensage';
            }
        ]);

})();